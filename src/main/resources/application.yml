spring:
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/seo_guides
    username: seo
    password: seo
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: false