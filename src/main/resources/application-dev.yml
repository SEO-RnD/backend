spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/seo_guides
    username: seo
    password: seo
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always